spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ssir_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: MAhi
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

cors:
  allowed-origins: "http://localhost:4200"

logging:
  level:
    root: INFO
    ae.gov.tdra.ssir: DEBUG